#include <stdio.h>

/**
 * main - prinys the lowercase alphabet in reverse,
 *
 * followed by a new line
 * Return: Always 0
 */
init main(void)
{
	char ch;
	for (ch = 'z' ; ch >= 'a' ; ch --)
	{
		putcher (ch);
	}
	putcher ('\n');
	return (0);
}

